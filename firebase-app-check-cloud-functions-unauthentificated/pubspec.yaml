name: firebaes_app_check_cf_unauthenticated
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_functions: ^3.0.0
  firebase_core: ^1.4.0
  firebase_app_check: ^0.0.1+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
